# Pipeline Health Report

_Generated 2026-07-24T12:31:00.959288+00:00_

This is a real, committed output of `scripts/pipeline_health_check.py`, generated by actually running the full Bronze -> Silver -> Gold pipeline in this repo (producer -> PySpark Silver jobs -> dbt build) so a reviewer can see genuine pipeline output without running anything themselves. Regenerate it locally with `python scripts/pipeline_health_check.py` after your own `dbt build`.

## Gold layer row counts

| Table | Row count |
|---|---|
| `main_marts.dim_customer_scd2` | 2433 |
| `main_marts.dim_product` | 120 |
| `main_marts.dim_store` | 15 |
| `main_marts.dim_date` | 1461 |
| `main_marts.fct_sales` | 14940 |
| `main_marts.mart_daily_store_sales` | 18 |
| `main_marts.mart_customer_ltv` | 2000 |

## Silver validation

- Quarantined (failed-validation) raw sales events: **104**

## dbt test summary

- success: 12
- pass: 51

## Notes

- SCD2 note: `dim_customer_scd2` has 2,433 rows for 2,000 distinct customers -- 433 customers had at least one tracked-attribute change (loyalty_tier or home_store_id) across the 3 simulated ingest dates in this sample run. See `gold/sample_output/dim_customer_scd2_changed_customers.csv`.
- `gold/sample_output/` contains CSV exports of key marts so the Gold layer output is browsable without running DuckDB.
